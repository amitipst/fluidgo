# DO NOT commit real values here. This file ships with placeholder base64 values only —
# regenerate DB_PASSWORD and JWT_SECRET before any real deployment:
#   echo -n "your_strong_password" | base64
#   python -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
# Then either edit this file locally (never commit real secrets) or, better, manage it with
# `kubectl create secret generic fluidgo-secrets -n fluidgo --from-literal=... --dry-run=client -o yaml`
# and pipe straight into `kubectl apply -f -` so real values never touch git.
apiVersion: v1
kind: Secret
metadata:
  name: fluidgo-secrets
  namespace: fluidgo
type: Opaque
data:
  DB_PASSWORD: ZmxVaWRnb19wcm9kXzIwMjY=
  JWT_SECRET: Q0hBTkdFX01FX3N0cm9uZ19qd3Rfc2VjcmV0X2hlcmU=
